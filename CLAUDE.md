# Claude Code Instructions

Shared project instructions are centralized in `AGENTS.md` and `.agents/`.

Before making changes, read:

1. `AGENTS.md`
2. `.agents/README.md`
3. The relevant focused file under `.agents/`

Do not maintain separate project rules in this file. Update the shared agent
root instead.
